# Normalization Utils

Pure string, case, date-part, and filename timestamp helpers for shared app use.

This package intentionally excludes ontology/RDF validation. Ontology input
classification remains in `ontology-utils`.
