# Ontology Utils Fixtures

Current Jest coverage uses inline fixtures because the paradigmatic cases are
small RDF/JS terms, short IRI tokens, and short ontology text snippets.

Add file fixtures here if later app migrations need larger ontology samples or
datatype tables.
